# Railway deployment configuration for n8n Value Investing Pipeline

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
numReplicas = 1
startCommand = "n8n start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
